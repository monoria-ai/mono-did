---
"@mono/did": minor
---

Add a unified `@mono/did` aggregate entrypoint that re-exports identity, handshake, protocol, adapter, and core types packages.

This enables one-package installation (`npm i @mono/did`) for downstream consumers.
